def check_for_updates():
    current_version = "1.0.0"
    latest_version = "1.1.0" # Ye kisi API se bhi aa sakta hai

    if current_version < latest_version:
        print(f"🚀 Update available! New version: {latest_version}")
        print("Please run 'git pull' to get the latest changes.")
    else:
        print("✅ You are using the latest version.")

if __name__ == "__main__":
    check_for_updates() 
